# Host completo de la API (sin barra final). Ej: https://casino-innova-api.onrender.com
# Dejalo VACIO en desarrollo: las rutas quedan relativas y las resuelve el
# proxy de Vite hacia http://localhost:4000 (ver server.proxy en vite.config.ts).
VITE_API_URL=
